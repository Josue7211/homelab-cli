---
phase: 07-qbittorrent
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified: [bin/qbt]
autonomous: true
requirements: [QBT-03, QBT-04, QBT-05, QBT-06, QBT-07, QBT-08]

must_haves:
  truths:
    - "User can set per-file download priority (skip/normal/high/max) for a torrent"
    - "User can view or set global download speed limit"
    - "User can view or set global upload speed limit"
    - "User can list all categories with save paths"
    - "User can assign a torrent to a category"
    - "User can move torrent files to a new location"
  artifacts:
    - path: "bin/qbt"
      provides: "cmd_priority, cmd_limit_dl, cmd_limit_ul, cmd_categories, cmd_set_category, cmd_move functions"
      contains: "cmd_priority"
  key_links:
    - from: "bin/qbt"
      to: "qBittorrent API /api/v2/torrents/filePrio, /api/v2/transfer/downloadLimit, /api/v2/transfer/setDownloadLimit, /api/v2/transfer/uploadLimit, /api/v2/transfer/setUploadLimit, /api/v2/torrents/categories, /api/v2/torrents/setCategory, /api/v2/torrents/setLocation"
      via: "qbt_get and qbt_post calls"
      pattern: "qbt_post.*(filePrio|setDownloadLimit|setUploadLimit|setCategory|setLocation)"
---

<objective>
Add torrent management commands (priority, move, set-category) and speed limit commands (limit-dl, limit-ul) and category listing to the qBittorrent CLI, plus update help text.

Purpose: Enable torrent management and speed control from the command line without the web UI.
Output: 6 new commands wired into bin/qbt main() case statement with updated help text.
</objective>

<context>
@.planning/phases/07-qbittorrent/07-CONTEXT.md
@bin/qbt (updated from 07-01)
@lib/common.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add management commands (priority, categories, set-category, move)</name>
  <files>bin/qbt</files>
  <read_first>
    - bin/qbt (updated from plan 01 -- get current line numbers and structure)
  </read_first>
  <acceptance_criteria>
    - grep -q "cmd_priority" bin/qbt
    - grep -q "cmd_categories" bin/qbt
    - grep -q "cmd_set_category" bin/qbt
    - grep -q "cmd_move" bin/qbt
    - grep -q "filePrio" bin/qbt
    - grep -q "setCategory" bin/qbt
    - grep -q "setLocation" bin/qbt
    - bash -n bin/qbt
  </acceptance_criteria>
  <action>
Add 4 functions before cmd_help():

**cmd_priority()** (D-03, QBT-03):
- Usage: qbt priority <hash|name> <file-index> <prio>
- prio values: 0=skip, 1=normal, 6=high, 7=max
- POST /api/v2/torrents/filePrio with hash, id (file index), priority

**cmd_categories()** (D-06, QBT-06):
- GET /api/v2/torrents/categories
- Response is object: {"catname": {"name": "...", "savePath": "..."}, ...}
- Table: name, save path

**cmd_set_category()** (D-07, QBT-07):
- Usage: qbt set-category <hash|name> <category>
- POST /api/v2/torrents/setCategory with hashes and category

**cmd_move()** (D-08, QBT-08):
- Usage: qbt move <hash|name> <path>
- POST /api/v2/torrents/setLocation with hashes and location

Wire all 4 into main() case statement before help.
  </action>
  <verify>
    <automated>bash -n bin/qbt && grep -q "cmd_priority" bin/qbt && grep -q "cmd_categories" bin/qbt && grep -q "cmd_set_category" bin/qbt && grep -q "cmd_move" bin/qbt && echo "PASS"</automated>
  </verify>
  <done>4 management commands wired: priority sets per-file download priority, categories lists all categories, set-category assigns torrent to category, move changes torrent save location.</done>
</task>

<task type="auto">
  <name>Task 2: Add speed limit commands (limit-dl, limit-ul) and update help text</name>
  <files>bin/qbt</files>
  <read_first>
    - bin/qbt (updated from Task 1)
  </read_first>
  <acceptance_criteria>
    - grep -q "cmd_limit_dl" bin/qbt
    - grep -q "cmd_limit_ul" bin/qbt
    - grep -q "downloadLimit" bin/qbt
    - grep -q "uploadLimit" bin/qbt
    - grep -q "setDownloadLimit" bin/qbt
    - grep -q "setUploadLimit" bin/qbt
    - grep -q "INSPECTION" bin/qbt
    - grep -q "MANAGEMENT" bin/qbt
    - grep -q "LIMITS" bin/qbt
    - bash -n bin/qbt
  </acceptance_criteria>
  <action>
Add 2 functions before cmd_help():

**cmd_limit_dl()** (D-04, QBT-04):
- With no arg: GET /api/v2/transfer/downloadLimit, show current limit (0=unlimited, else bytes/s)
- With arg: POST /api/v2/transfer/setDownloadLimit with limit= (value in bytes/s, 0=unlimited)

**cmd_limit_ul()** (D-05, QBT-05):
- With no arg: GET /api/v2/transfer/uploadLimit, show current limit
- With arg: POST /api/v2/transfer/setUploadLimit with limit=

Update cmd_help() to include all new commands organized into sections:
- STATUS: status, speed
- TORRENTS: list, add, pause, resume, delete
- INSPECTION: info, files, trackers, peers
- MANAGEMENT: priority, categories, set-category, move
- LIMITS: limit-dl, limit-ul

Wire limit-dl and limit-ul into main() case statement.
  </action>
  <verify>
    <automated>bash -n bin/qbt && grep -q "cmd_limit_dl" bin/qbt && grep -q "cmd_limit_ul" bin/qbt && grep -q "INSPECTION" bin/qbt && grep -q "MANAGEMENT" bin/qbt && grep -q "LIMITS" bin/qbt && echo "PASS"</automated>
  </verify>
  <done>Speed limit commands show/set global DL/UL limits. Help text updated with INSPECTION, MANAGEMENT, LIMITS sections covering all 18 commands.</done>
</task>

</tasks>

<verification>
All 6 commands syntax-valid and wired:
```bash
bash -n bin/qbt
grep -c "cmd_priority\|cmd_categories\|cmd_set_category\|cmd_move\|cmd_limit_dl\|cmd_limit_ul" bin/qbt
# Expect: 12+ matches (function defs + case entries)
```
</verification>

<success_criteria>
- bin/qbt passes bash -n syntax check
- 6 new functions exist: cmd_priority, cmd_categories, cmd_set_category, cmd_move, cmd_limit_dl, cmd_limit_ul
- 6 new case entries wired in main()
- Help text includes INSPECTION, MANAGEMENT, LIMITS sections
- All API paths match context decisions (D-03 through D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/07-qbittorrent/07-02-SUMMARY.md`
</output>
